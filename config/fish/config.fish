set -g fish_greeting ""
source ~/.cache/wal/colors.fish

# Toggle picom desktop animation (slide/fade). 'switch' is a fish keyword,
# so use: switch-btw   (or the short alias: anim)
alias anim='switch-btw'
